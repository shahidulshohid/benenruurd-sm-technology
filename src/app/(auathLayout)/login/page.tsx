import LoginForm from '@/components/auth/Login-form';
import React from 'react';

const Login = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default Login;